# Bildverarbeitung
Pillow>=10.0

# GUI (modernes Theming für tkinter)
customtkinter>=5.2

# Optional: Drag & Drop in der GUI
tkinterdnd2>=0.4

# PDF-Eingabe (PDF → Bild); Bild → PDF geht auch ohne (via Pillow)
PyMuPDF>=1.24
