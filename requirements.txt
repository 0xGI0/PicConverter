# Bildverarbeitung
Pillow>=10.0

# GUI (Qt für Python — bringt Drag & Drop und natives HiDPI mit)
PySide6>=6.6

# PDF- und SVG-Eingabe (→ Bild); Bild → PDF geht auch ohne (via Pillow)
PyMuPDF>=1.24

# Optional: HEIC/HEIF-Eingabe (iPhone-Fotos); AVIF kann Pillow ab v11 nativ
pillow-heif>=0.16
